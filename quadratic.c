#include<stdio.h>
#include<math.h>
int main(){
    float a,b,c,D,d;
    printf("Enter the valiue of a : ");
    scanf("%f", &a);
    printf("Enter the valiue of b : ");
    scanf("%f", &b);
    printf("Enter the valiue of c : ");
    scanf("%f", &c);
    D = (pow(b,2)-(4*a*c));

 
    if(d)
    {
        printf("");
    }
    return 0;
}

